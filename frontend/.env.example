# Local development: leave this unset — vite.config.js proxies /api to
# http://localhost:5000 automatically.
#
# Production (Vercel, Netlify, etc.): set this to your deployed backend's
# full URL, including the /api suffix, e.g.:
# VITE_API_URL=https://meridian-backend.onrender.com/api
